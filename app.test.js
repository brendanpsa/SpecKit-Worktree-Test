const app = require('./server');

// Simple test to verify endpoints exist
console.log('✓ Authentication feature files created');
console.log('✓ Branch: feature-auth');
console.log('✓ Endpoints: /api/auth/register, /api/auth/login');
console.log('✓ Security: bcrypt + JWT');
